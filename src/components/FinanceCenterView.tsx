import React,{useMemo,useState}from'react';import{FinancePlan,CashEntry,DebtRecord}from'../types';import{Wallet,CreditCard,Building2,User,AlertTriangle,Plus,X,Trash2,Settings2}from'lucide-react';
const money=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),today=()=>new Date().toISOString().slice(0,10);
export const FinanceCenterView:React.FC<{plan:FinancePlan;entries:CashEntry[];debts:DebtRecord[];onPlan:(p:FinancePlan)=>Promise<void>;onEntry:(x:any)=>Promise<void>;onDeleteEntry:(id:string)=>Promise<void>;onDebt:(x:any)=>Promise<void>;onDeleteDebt:(id:string)=>Promise<void>}>=({plan,entries,debts,onPlan,onEntry,onDeleteEntry,onDebt,onDeleteDebt})=>{
 const[p,setP]=useState(plan),[tab,setTab]=useState<'GERAL'|'EMPRESA'|'PESSOAL'|'DIVIDAS'|'MOVIMENTACOES'>('GERAL'),[entry,setEntry]=useState(false),[debt,setDebt]=useState(false);
 React.useEffect(()=>setP(plan),[plan]);
 const month=entries.filter(x=>x.date.slice(0,7)===today().slice(0,7)),personalIncome=month.filter(x=>x.area==='PERSONAL'&&x.kind==='INCOME').reduce((a,x)=>a+x.amount,0),personalSpend=month.filter(x=>x.area==='PERSONAL'&&x.kind==='EXPENSE').reduce((a,x)=>a+x.amount,0),spendLimit=personalIncome*p.personalSpendPct/100,debtTotal=debts.reduce((a,x)=>a+x.balance,0),status=spendLimit&&personalSpend>spendLimit?'VERMELHO':spendLimit&&personalSpend>spendLimit*.8?'AMARELO':'VERDE';
 const cards=[['Mercado Pago',p.mercadoPagoBalance,Building2],['Caixa empresa',p.businessCash,Wallet],['Caixa pessoal',p.personalCash,User],['Dívidas',debtTotal,CreditCard]];
 return <div className="space-y-5 max-w-[1400px] mx-auto"><div><h1 className="text-2xl font-black">Centro Financeiro</h1><p className="text-sm text-slate-500">Empresa e vida pessoal separadas, com planejamento e limites.</p></div>
 <div className="flex gap-2 overflow-x-auto pb-1">{[['GERAL','Visão Geral'],['EMPRESA','Empresa / ML'],['PESSOAL','Pessoal'],['DIVIDAS','Dívidas'],['MOVIMENTACOES','Movimentações']].map(([x,l])=><button key={x} onClick={()=>setTab(x as any)} className={`whitespace-nowrap px-4 py-2 rounded-xl text-xs font-black ${tab===x?'bg-amber-400 text-slate-950':'bg-white dark:bg-slate-900 border dark:border-slate-800'}`}>{l}</button>)}</div>
 {(tab==='GERAL'||tab==='EMPRESA'||tab==='PESSOAL')&&<><div className="grid grid-cols-2 lg:grid-cols-4 gap-3">{cards.map(([l,v,I]:any)=><div className="p-4 rounded-2xl bg-white dark:bg-slate-900 border dark:border-slate-800" key={l}><div className="text-xs text-slate-500 font-bold flex gap-2"><I className="w-4 h-4"/>{l}</div><div className="text-xl font-black mt-2">{money(v)}</div></div>)}</div>
 <div className={`rounded-2xl border p-4 ${status==='VERMELHO'?'border-rose-400 bg-rose-50 dark:bg-rose-950/20':status==='AMARELO'?'border-amber-400 bg-amber-50 dark:bg-amber-950/20':'border-emerald-400 bg-emerald-50 dark:bg-emerald-950/20'}`}><div className="flex items-center gap-2 font-black"><AlertTriangle className="w-5 h-5"/>Semáforo financeiro: {status==='VERDE'?'SAUDÁVEL':status==='AMARELO'?'ATENÇÃO':'LIMITE ULTRAPASSADO'}</div><p className="text-xs mt-1 text-slate-600 dark:text-slate-400">Gastos pessoais do mês: {money(personalSpend)} de {money(spendLimit)} permitidos ({p.personalSpendPct}%).</p></div>
 <section className="p-5 rounded-2xl bg-white dark:bg-slate-900 border dark:border-slate-800"><div className="flex justify-between items-center"><div><h2 className="font-black">Regra do dinheiro</h2><p className="text-xs text-slate-500">Você define os percentuais. O sistema apenas acompanha os limites.</p></div><Settings2 className="text-amber-500"/></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 mt-4">{[['Gastos pessoais','personalSpendPct'],['Dívidas','debtPct'],['Reserva','reservePct'],['Investimentos','investPct']].map(([l,k])=><label className="text-xs font-bold" key={k}>{l} %<input type="number" value={(p as any)[k]} onChange={e=>setP({...p,[k]:Number(e.target.value)||0})} className="mt-1 w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></label>)}</div><div className="grid sm:grid-cols-4 gap-3 mt-3"><label className="text-xs font-bold">Saldo Mercado Pago<input type="number" value={p.mercadoPagoBalance} onChange={e=>setP({...p,mercadoPagoBalance:Number(e.target.value)||0})} className="mt-1 w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></label><label className="text-xs font-bold">Caixa empresa<input type="number" value={p.businessCash} onChange={e=>setP({...p,businessCash:Number(e.target.value)||0})} className="mt-1 w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></label><label className="text-xs font-bold">Caixa pessoal<input type="number" value={p.personalCash} onChange={e=>setP({...p,personalCash:Number(e.target.value)||0})} className="mt-1 w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></label><label className="text-xs font-bold">Reinvestir empresa %<input type="number" value={p.businessReinvestPct} onChange={e=>setP({...p,businessReinvestPct:Number(e.target.value)||0})} className="mt-1 w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></label></div><button onClick={()=>onPlan(p)} className="mt-4 bg-amber-400 text-slate-950 font-black px-5 py-2.5 rounded-xl">Salvar planejamento</button></section></>}
 {tab==='DIVIDAS'&&<section className="rounded-2xl bg-white dark:bg-slate-900 border dark:border-slate-800 overflow-hidden"><div className="p-4 flex justify-between"><div><h2 className="font-black">Controle de dívidas</h2><p className="text-xs text-slate-500">Saldo devedor, parcela e vencimento.</p></div><button onClick={()=>setDebt(true)} className="bg-amber-400 text-slate-950 font-black px-3 py-2 rounded-xl text-xs"><Plus className="inline w-4 h-4"/> Dívida</button></div>{debts.map(d=><div key={d.id} className="p-4 border-t dark:border-slate-800 flex justify-between"><div><b>{d.name}</b><p className="text-xs text-slate-500">Saldo {money(d.balance)} • Parcela {money(d.installment)} • vence dia {d.dueDay}</p></div><button onClick={()=>onDeleteDebt(d.id)} className="text-rose-500"><Trash2 className="w-4 h-4"/></button></div>)}</section>}
 {tab==='MOVIMENTACOES'&&<section className="rounded-2xl bg-white dark:bg-slate-900 border dark:border-slate-800 overflow-hidden"><div className="p-4 flex justify-between"><div><h2 className="font-black">Movimentações financeiras</h2><p className="text-xs text-slate-500">Entradas, gastos, investimentos e pagamentos.</p></div><button onClick={()=>setEntry(true)} className="bg-amber-400 text-slate-950 font-black px-3 py-2 rounded-xl text-xs"><Plus className="inline w-4 h-4"/> Lançar</button></div>{entries.map(e=><div key={e.id} className="p-4 border-t dark:border-slate-800 grid grid-cols-[1fr_auto]"><div><b>{e.description}</b><p className="text-xs text-slate-500">{e.area==='BUSINESS'?'Empresa':'Pessoal'} • {e.category} • {new Date(e.date+'T12:00').toLocaleDateString('pt-BR')}</p></div><div className="flex gap-3 items-center"><b>{money(e.amount)}</b><button onClick={()=>onDeleteEntry(e.id)} className="text-rose-500"><Trash2 className="w-4 h-4"/></button></div></div>)}</section>}
 {entry&&<EntryModal close={()=>setEntry(false)} save={async x=>{await onEntry(x);setEntry(false)}}/>}{debt&&<DebtModal close={()=>setDebt(false)} save={async x=>{await onDebt(x);setDebt(false)}}/>}</div>}
const EntryModal=({close,save}:any)=>{const[x,setX]=useState({area:'PERSONAL',kind:'EXPENSE',category:'Geral',description:'',amount:0,date:today()});return <div className="fixed inset-0 z-[100] bg-slate-950/70 grid place-items-center p-3"><div className="w-full max-w-md bg-white dark:bg-slate-900 rounded-2xl p-5 space-y-3"><div className="flex justify-between"><b>Novo lançamento</b><button onClick={close}><X/></button></div><div className="grid grid-cols-2 gap-2"><select value={x.area} onChange={e=>setX({...x,area:e.target.value})} className="p-3 rounded-xl border dark:border-slate-700 bg-white dark:bg-slate-900"><option value="PERSONAL">Pessoal</option><option value="BUSINESS">Empresa</option></select><select value={x.kind} onChange={e=>setX({...x,kind:e.target.value})} className="p-3 rounded-xl border dark:border-slate-700 bg-white dark:bg-slate-900"><option value="INCOME">Entrada</option><option value="EXPENSE">Gasto</option><option value="INVESTMENT">Investimento</option><option value="DEBT_PAYMENT">Pagamento dívida</option></select></div><input placeholder="Descrição" value={x.description} onChange={e=>setX({...x,description:e.target.value})} className="w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><input placeholder="Categoria" value={x.category} onChange={e=>setX({...x,category:e.target.value})} className="w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><div className="grid grid-cols-2 gap-2"><input type="number" placeholder="Valor" onChange={e=>setX({...x,amount:Number(e.target.value)||0})} className="p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><input type="date" value={x.date} onChange={e=>setX({...x,date:e.target.value})} className="p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></div><button onClick={()=>save(x)} className="w-full bg-amber-400 text-slate-950 font-black py-3 rounded-xl">Salvar</button></div></div>}
const DebtModal=({close,save}:any)=>{const[x,setX]=useState({name:'',balance:0,installment:0,dueDay:1,note:''});return <div className="fixed inset-0 z-[100] bg-slate-950/70 grid place-items-center p-3"><div className="w-full max-w-md bg-white dark:bg-slate-900 rounded-2xl p-5 space-y-3"><div className="flex justify-between"><b>Cadastrar dívida</b><button onClick={close}><X/></button></div><input placeholder="Credor / nome da dívida" onChange={e=>setX({...x,name:e.target.value})} className="w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><div className="grid grid-cols-2 gap-2"><input type="number" placeholder="Saldo devedor" onChange={e=>setX({...x,balance:Number(e.target.value)||0})} className="p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><input type="number" placeholder="Parcela" onChange={e=>setX({...x,installment:Number(e.target.value)||0})} className="p-3 rounded-xl border dark:border-slate-700 bg-transparent"/></div><input type="number" min="1" max="31" placeholder="Dia vencimento" onChange={e=>setX({...x,dueDay:Number(e.target.value)||1})} className="w-full p-3 rounded-xl border dark:border-slate-700 bg-transparent"/><button onClick={()=>save(x)} className="w-full bg-amber-400 text-slate-950 font-black py-3 rounded-xl">Salvar dívida</button></div></div>}
